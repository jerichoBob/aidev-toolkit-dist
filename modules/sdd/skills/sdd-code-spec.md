---
name: sdd-code-spec
tier: extended
deprecated: true
description: "Deprecated — use /sdd-code instead. Forwards to /sdd-code for backward compatibility."
argument-hint: "[spec-version] [--no-stats]"
allowed-tools: Skill
---

# /sdd-code-spec (Deprecated)

`/sdd-code-spec` is deprecated. Use `/sdd-code` instead.

## Instructions

1. Print this warning:

   ```text
   ⚠️  `/sdd-code-spec` is deprecated. Use `/sdd-code` instead.
   ```

2. Forward all arguments to `/sdd-code` via the Skill tool, passing `$ARGUMENTS` unchanged.
